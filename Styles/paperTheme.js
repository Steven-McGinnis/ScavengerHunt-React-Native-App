import { DefaultTheme } from 'react-native-paper';

// Version 1.0.0
export const customTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: 'white',
        accent: 'white',
        text: 'white',
    },
};
